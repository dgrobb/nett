// A correctly named enum
enum CorrectName {
    ONE, TWO, THREE
};

enum incorrectCamelCase;

// Here's the definition
enum incorrectCamelCase {
    FOUR, FIVE, SIX
};

enum Some_snake_case {
    SEVEN, 
    EIGHT
};